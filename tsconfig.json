{
  "compilerOptions": {
    // куда производится сборка
    "outDir": "./dist/",
    // подсвесчивает места, где нужен тип
    "noImplicitAny": true,

     "module": "esnext", 
    // в какую версию компилируется код для поддержки большинства браузеров
    "target": "es5",
    "jsx": "react-jsx",
    // позволяет обрабатывать js файлы
    "allowJs": true,
    // 
    "moduleResolution": "node",
    "baseUrl": ".",
    // позволяет работать с пакетами, которые используют common js как с обычными пакетами с помощью import (require() module.export) = common js
    "esModuleInterop": true,
    // если в библиотке не имеет дефолтный импорт, лоадеры их автоматически создают 
    "allowSyntheticDefaultImports": true,
    
  },
     "ts-node": {
        "compilerOptions": {
            "module": "CommonJS"
        }
    }   
}